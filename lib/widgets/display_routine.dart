import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class DisplayRoutine extends StatelessWidget {
  const DisplayRoutine({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
